config = {
    port: 3002,
    baseUrl: "/backend",
    mysql: {
        host: "localhost",
        port: 3306,
        password: "password",
        user: "root",
        database: "websitewebsite"
      },
}

module.exports = config;